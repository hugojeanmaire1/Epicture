import * as React from 'react';
import { Image, Text, View, StyleSheet } from 'react-native'

class Comments extends React.Component {
    render() {
        <Image>{this.props.comment_img}</Image>
        <Text>{this.props.comment}</Text>
    }
}
